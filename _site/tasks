[ ] how to make the bottom border on the site (currently maroon) different colors: what is that color variable called and in which file is it located?
[ ] same question for the color of the hover bars on the masthead
[ ] make the twitter feed scroll (so that the page doesn't continue to get longer as the feed extends)
